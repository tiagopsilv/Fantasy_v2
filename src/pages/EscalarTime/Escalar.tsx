// D:\Fantasy_2\src\pages\EscalarTime\Escalar.tsx

import React from "react";
import App from "./App";

/**
 * Componente legado que só delega para o novo App.
 * Serve para qualquer import antigo de `Escalar`.
 */
const Escalar: React.FC = () => {
  return <App />;
};

export default Escalar;